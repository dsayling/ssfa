version: 2.1
orbs:
        ruby: circleci/ruby@dev:fix-install

jobs:
  sample:
    machine:
      image: ubuntu-1604:202004-01
    steps:
      - run: echo "deploy"
      - run: ruby -v
      - ruby/install:
          version: '2.7.0'
      - run: ruby -v | grep '2.7.1'

workflows:
  sample_wf:
    jobs:
      - sample
