version: 2.1
orbs:
        ruby: circleci/ruby@dev:fix-install

jobs:
  sample:
    machine:
      image: ubuntu-1604:202010-01
    steps:
      python --version
      pyenv global
      pyenv versions

workflows:
  sample_wf:
    jobs:
      - sample
