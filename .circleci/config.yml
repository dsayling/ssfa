version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: apk add bash && sed -i 's_/ash_/bash_g' /etc/passwd
      - run: echo "CHICKEN=you" >> $BASH_ENV
      - run: echo $CHICKEN
