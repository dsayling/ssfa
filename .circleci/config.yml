version: 2.1
workflows:
  main:
    jobs:
      - build2
jobs:
  build2:
    docker:
      - image: cimg/base:2020.12
    working_directory: ~/project
    steps:
      - run: echo $CIRCLE_WORKING_DIRECTORY && echo $PWD
      - run: mkdir -p src/test
      - run:
          command: echo $CIRCLE_WORKING_DIRECTORY && echo $PWD
          working_directory: ~/project/src/test
