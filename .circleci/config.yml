version: 2.1

orbs:
  node: circleci/node@4.0.0

workflows:
  sample:
    jobs:
      - build

jobs:
  build:
    # executor: node/default
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "build"
      - run: git describe --exact-match | sed -nre 's/^[^0-9]*(([0-9]+\.)*[0-9]+).*/\1/p'
