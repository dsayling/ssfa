version: 2.1
orbs:
        ruby: circleci/ruby@dev:fix-install

jobs:
  sample:
    machine:
      image: ubuntu-1604:202004-01
    steps:
      - run: ruby -v
      - ruby/install:
          version: '2.6.6'
      - run: ruby -v | grep '2.6.6'

workflows:
  sample_wf:
    jobs:
      - sample
