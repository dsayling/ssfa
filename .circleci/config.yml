version: 2.1
orbs:
        ruby: circleci/ruby@dev:fix-install

jobs:
  sample:
    machine:
      image: ubuntu-1604:202004-01
    steps:
      - run: |
          set -x
          python --version
          python3 --version
          python3 -c 'import lzma; print(dir(lzma))'
          python -c 'import lzma; print dir(lzma)'

workflows:
  sample_wf:
    jobs:
      - sample
