version: 2.1
workflows:
  main:
    jobs:
      - build
jobs:
  build:
    docker:
      - image: alpine:3.7
    steps:
      - checkout
      - run: apk add bash && ln -s /bin/bash /bin/sh -f
      - run: echo "CHICKEN=you" >> $BASH_ENV
      - run: echo $CHICKEN
