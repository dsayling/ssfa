version: 2.1

orbs:
  node: circleci/node@4.0.0

wokflows:
  sample:
    jobs:
      - build
      - style
      - test

jobs:
  build:
    executor: node/default
    steps:
      - checkout
      - run: echo "build"

  style:
    executor: node/default
    steps:
      - checkout
      - run: echo "style"

  test:
    executor: node/default
    steps:
      - checkout
      - run: echo "test"

