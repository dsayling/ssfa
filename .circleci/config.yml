version: 2.1

orbs:
  gcp-cli: circleci/gcp-cli@1.8
jobs: 
  sample:
    machine:
      image: ubuntu-2004:202010-01
    steps:
      - run: echo $PATH
      - run: pyenv --help
      - run: echo "export PATH=$PATH:$PWD" >> $BASH_ENV
      - run: pyenv --help

workflows:
  sample_wf: # this is the name of our workflow
    jobs: # and here we list the jobs we are going to run.
      - sample
