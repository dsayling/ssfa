version: 2.1

wokflows:
  sample:
    jobs:
      - build
      - style
      - test

jobs:
  build:
    executor: node/default
    steps:
      - checkout

  style:
    executor: node/default
    steps:
      - checkout

  test:
    executor: node/default
    steps:
      - checkout

orbs:
  node: circleci/node@4.0.0
