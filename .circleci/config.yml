version: 2.1

orbs:
  node: circleci/node@4.0.0

workflows:
  sample:
    jobs:
      - build
      - style
      - test

jobs:
  build:
    # executor: node/default
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "build"

  style:
    # executor: node/default
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "style"

  test:
    # executor: node/default
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "test"

